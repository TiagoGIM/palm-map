{
  "name": "@palm-map/api",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev worker.ts --local --port 3001",
    "dev:staging": "wrangler dev worker.ts --local --port 3001 --env staging",
    "deploy:staging": "wrangler deploy --env staging",
    "test:plan-trip-validation": "TS_NODE_PROJECT=tsconfig.json node --loader ts-node/esm --experimental-specifier-resolution=node scripts/validate-plan-trip.ts"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "wrangler": "^4.41.0"
  }
}
