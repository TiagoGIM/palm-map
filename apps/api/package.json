{
  "name": "@palm-map/api",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev worker.ts --local --port 3001",
    "dev:staging": "wrangler dev worker.ts --local --port 3001 --env staging",
    "deploy:staging": "wrangler deploy --env staging"
  },
  "devDependencies": {
    "wrangler": "^4.41.0"
  }
}
